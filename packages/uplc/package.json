{
  "name": "@lucid-evolution/uplc",
  "version": "0.2.5",
  "main": "./dist/node/uplc_tx.js",
  "browser": "./dist/browser/uplc_tx.js",
  "types": "./dist/node/uplc_tx.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": [
    "./uplc_tx.js",
    "./snippets/*"
  ],
  "scripts": {
    "build-local": "rm -rf dist && cd rust ;export CC=emcc; export AR=emar; EMCC_CFLAGS='-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s STANDALONE_WASM=1 -s EXPORTED_FUNCTIONS=['_apply_params_to_script','_eval_phase_two_raw'] --no-entry -fvisibility=default' cargo build --target wasm32-unknown-emscripten --release;wasm-bindgen --target web --out-dir ../src/browser target/wasm32-unknown-emscripten/release/uplc_tx.wasm && wasm-bindgen --target nodejs --out-dir ../src/node target/wasm32-unknown-emscripten/release/uplc_tx.wasm",
    "build": "cp -r src dist",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  }
}
