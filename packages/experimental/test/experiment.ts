import { Console, Effect } from "effect";
// import { CML, FormatError } from "../src/index.js";

// try {
//   CML.AssetName.from_hex("ss").pipe(
//     Effect.andThen((a) => Console.log(a.to_str())),
//     Effect.runSync
//   );
//   // AssetName.from_str("ss").pipe(Effect.runSync);
//   // AssetName.from_cbor_hex("deadbeef").pipe(Effect.runSync);
//   // AssetName.from_json("deadbeef").pipe(Effect.runSync);
// } catch (error) {
//   console.log(error);
// }

// try {
//   type ModeOptions = {
//     mode?: "min" | "max" | "average";
//   };

//   // Usage with optional parameter
//   const calculate = (value: Array<number>, options: ModeOptions = {}) => {
//     const { mode = "average" } = options;
//     return mode;
//     // ... implementation
//   };
// } catch (error) {
//   console.log(error);
// }
